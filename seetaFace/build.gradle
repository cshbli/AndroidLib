apply plugin: 'com.android.library'

android {
    compileSdkVersion 25

    defaultConfig {
        minSdkVersion 22
        targetSdkVersion 22

        /*
        ndk {
            moduleName "seeta_facedet_lib"
        }
        */
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
    /*
    externalNativeBuild {
        cmake {
            path 'CMakeLists.txt'
        }
    }
    */
}

dependencies {
    compile project(':openCVLibrary310')
}
